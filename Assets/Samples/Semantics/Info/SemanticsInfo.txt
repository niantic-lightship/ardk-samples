In this example, We set up a shader that colors only what we recognize as part of the <i>semantic channel</i>.

To start, push the settings button, and select a semantic channel in the drop down.
Everything in the camera frame that matches the channel selected should be highlighted.